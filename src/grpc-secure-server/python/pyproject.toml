[project]
name = 'grpc-secure-server'
version = '0.0.0'
requires-python = '>= 3.10'

dependencies = [
    'protobuf >= 4.25',
    'grpcio >= 1.78.1',
    'grpcio-tools >= 1.78.1',
]

[tool.uv]
package = false
