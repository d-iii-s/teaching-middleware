# Java RMI Transparency Experiments

The example performs various simple calls that demonstrate the transparency of the RMI invocations.

## Running the example

```shell
mvn compile
mvn exec:java@server &
mvn exec:java@client
```
